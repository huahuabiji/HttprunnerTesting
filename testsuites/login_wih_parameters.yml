
config:
    name: "login with different accounts"  # 可重新命名/Users/bjhl/demo2/testsuites/login_wih_parameters.yml

testcases:
-
    name: login system
    testcase: testcases/login.yml  # 调用用例里边的登录脚本
    parameters:
        phone-password: ${P(data/accounts.csv)}  # 把phone和password使用csv文件，使登录的用户名和密码可以实现不同数据的参数化